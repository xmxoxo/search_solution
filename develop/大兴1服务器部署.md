# 大兴1服务器部署

## 服务器信息

核心应用：智能匹配系统 向量数据库 Milvus 

堡垒机登录地址：

https://js-safe.1633.com/core/auth/login/

帐号：

usr: hexi
pwd: Www.1633.com


大兴-应用服务器：

公网IP: 114.215.179.77
内网IP: 172.30.1.110

本机redis服务：

pwd: k8008.com

## 系统环境


硬件检测：

```
[root@daxing-data01 ~]# uname --all
Linux daxing-data01 4.18.0-348.7.1.el8_5.x86_64 #1 SMP Wed Dec 22 13:25:12 UTC 2021 x86_64 x86_64 x86_64 GNU/Linux

echo "CPU 核心: $(nproc) | 内存: $(free -h | awk '/Mem:/ {print $2}') | \
硬盘: $(lsblk -d -o NAME,SIZE,TYPE |grep disk | awk '{print $1 ":" $2}' | paste -sd ', ') | \
系统: $(grep PRETTY_NAME /etc/os-release | cut -d '"' -f 2)"
```

CPU 核心: 4 | 内存: 15Gi | 硬盘: vda:40G,vdb:300G | 系统: CentOS Linux 8

查看磁盘空间：

```
df -h --exclude-type=overlay --exclude-type=tmpfs

Filesystem      Size  Used Avail Use% Mounted on
devtmpfs        7.6G     0  7.6G   0% /dev
/dev/vda1        40G   15G   26G  38% /
/dev/vdb1       300G   76G  225G  26% /opt
```

## 部署 docker

已安装了docker:
```
[root@daxing-data01 ~]# docker --version
Docker version 20.10.12, build e91ed57
```

## 安装 docker-compose

```
# 下载最新版本的 docker-compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# 添加执行权限
chmod +x /usr/local/bin/docker-compose

# 创建软链接（可选，为了全局可用）
ln -s /usr/local/bin/docker-compose /usr/bin/docker-compose

# 验证安装
docker-compose --version
```

新版本下载：
```
https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-linux-x86_64
```

```
# 定义 Docker Compose 版本（可根据需要修改）
COMPOSE_VERSION="v2.24.0"
COMPOSE_BINARY_URL="https://github.com/docker/compose/releases/download/${COMPOSE_VERSION}/docker-compose-linux-x86_64"

# 尝试下载 Docker Compose 二进制
if curl -L "${COMPOSE_BINARY_URL}" -o /usr/local/bin/docker-compose; then
    chmod +x /usr/local/bin/docker-compose
    echo -e "${GREEN}Docker Compose 二进制安装成功${NC}"
else
    echo -e "${RED}下载 Docker Compose 二进制失败，尝试安装插件版本...${NC}"
    # 安装 docker-compose-plugin
    yum install -y docker-compose-plugin
    # 创建包装脚本，使 docker-compose 命令可用
    cat > /usr/local/bin/docker-compose << 'EOF'
#!/bin/bash
docker compose "$@"
EOF
    chmod +x /usr/local/bin/docker-compose
    echo -e "${GREEN}Docker Compose 插件安装成功（通过包装脚本）${NC}"
fi

cp docker-compose-linux-x86_64 /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# 验证 Docker Compose
docker-compose --version
```

运行结果：
```
[root@daxing-data01 tmp]# /usr/local/bin/docker-compose --version
Docker Compose version v2.24.0
[root@daxing-data01 tmp]# docker-compose --version
bash: docker-compose: command not found
```

检查：

```
echo $PATH

/sbin:/bin:/usr/sbin:/usr/bin
```

添加路径：
```
echo 'export PATH=$PATH:/usr/local/bin' >> ~/.bashrc
source ~/.bashrc
```

查看版本：
```
[root@daxing-data01 ~]# docker-compose --version
Docker Compose version v2.24.0
```

安装插件：

```
# 手动安装 compose 插件
mkdir -p ~/.docker/cli-plugins/
curl -SL https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-linux-x86_64 -o ~/.docker/cli-plugins/docker-compose

# 手工复制：
cp /usr/local/bin/docker-compose ~/.docker/cli-plugins/docker-compose

# 赋予执行权限
chmod +x ~/.docker/cli-plugins/docker-compose
ls ~/.docker/cli-plugins/docker-compose -all

# 验证
docker compose version
```

## 安装 portainer

### 注册工具
```
cd /tmp/system/

sudo chmod +x pullali.sh
sudo cp pullali.sh /usr/local/bin/
sudo ln -s /usr/local/bin/pullali.sh /usr/local/bin/pullali
```

拉取镜像
```
docker pull portainer/portainer-ce:2.39.0

pubtoali portainer/portainer-ce:2.39.0
pullali portainer/portainer-ce:2.39.0


# 创建卷
docker volume create portainer_data

# 启动容器
sudo docker run -d -p 9000:9000 \
--name portainer \
--restart always \
-v /var/run/docker.sock:/var/run/docker.sock \
-v portainer_data:/data portainer/portainer-ce:2.39.0
```

访问地址:  `http://114.215.179.77:9000`


帐号：
```
user: admin
pwd: Www.1633.com
```

## 安装部署Milvus 

版本：2.6.11

目录：`/opt/milvus_2.6.11`
创建目录：
```
mkdir -p /opt/milvus_2.6.11
cd /opt/milvus_2.6.11
```

下载配置文件：
```
wget https://github.com/milvus-io/milvus/releases/download/v2.6.11/milvus-standalone-docker-compose.yml -O docker-compose.yml
```

修改端口避免与 portainer 冲突：
```
F:\project\通用语义匹配引擎\milvus\Milvus_install\milvus_2.6.11
```

```
docker pull quay.io/coreos/etcd:v3.5.25

pubtoali minio/minio:RELEASE.2024-12-18T13-15-44Z
pullali minio/minio:RELEASE.2024-12-18T13-15-44Z


docker pull milvusdb/milvus:v2.6.11
pubtoali milvusdb/milvus:v2.6.11
pullali milvusdb/milvus:v2.6.11
```

启动容器：
```
cd /opt/milvus_2.6.11
sudo docker compose up -d
```

服务地址： `114.215.179.77:19530`

## 恢复数据

工作目录：
```
mkdir -p /opt/app/ime_app
cd /opt/app/ime_app
```

复制相关文件：
```

mv /tmp/config/* config/
mv /tmp/data/* data/
```

服务器上安装screen:
```
yum install screen 
```


拉取镜像：
```
docker pull registry.cn-hangzhou.aliyuncs.com/xmxoxo_test/ime-app:0.1.0
docker tag registry.cn-hangzhou.aliyuncs.com/xmxoxo_test/ime-app:0.1.0 ime-app:0.1.0
```


启动容器进行测试：无服务，映射目录用于工具调用；
```
cd /opt/app/ime_app

docker run \
  -v "$(pwd)/data":/app/data \
  -v $(pwd)/config:/app/config \
  -v $(pwd)/log:/app/log \
  --name ime_tool \
  -it ime-app:0.1.0 \
  /bin/bash

docker start -ai ime_tool

docker exec -it ime_tool /bin/bash
docker attach ime_tool


# docker run -it -name ime_tool ime_tool:0.1.0 /bin/bash

```



原来导入过程的数据保存了中间数据文件，数据文件在："data/*.csv"

```
F:\project\search_solution\data>dir patent_CN*  /b
patent_CN00100160.4_patent.csv
patent_CN00100545.6_patent.csv
patent_CN00127936.X_patent.csv
patent_CN200410091848.6_patent.csv
patent_CN202020414146.1_patent.csv
patent_CN202111391453.8_patent.csv
patent_CN202220760857.3_patent.csv
patent_CN202230489461.5_patent.csv
```

导入命令见：`dev_v2.md#26.4 使用方法`

进入容器后执行：

```
docker attach ime_tool
```

从中间数据文件恢复:
```
cd /app

查看集合：
python tools/manage_collection.py --list

导入数据
python tools/import_mysql_patent.py --from-file data/patent_CN00100160.4_patent.csv --resource patent
python tools/import_mysql_patent.py --from-file data/patent_CN00100545.6_patent.csv --resource patent
python tools/import_mysql_patent.py --from-file data/patent_CN00127936.X_patent.csv --resource patent
python tools/import_mysql_patent.py --from-file data/patent_CN200410091848.6_patent.csv --resource patent
python tools/import_mysql_patent.py --from-file data/patent_CN202020414146.1_patent.csv --resource patent

python tools/import_mysql_patent.py --from-file data/patent_CN202111391453.8_patent.csv --resource patent; \
python tools/import_mysql_patent.py --from-file data/patent_CN202220760857.3_patent.csv --resource patent; \
python tools/import_mysql_patent.py --from-file data/patent_CN202230489461.5_patent.csv --resource patent
```



继续导数据：

```
python3 tools/import_mysql_patent.py --start-id="CN202020414127.9" --limit 300000
python3 tools/import_mysql_patent.py --start-id "CN202111391432.6" --limit 300000
python3 tools/import_mysql_patent.py --start-id "CN202220760856.9" --limit 120000
python3 tools/import_mysql_patent.py --start-id "CN202230489442.2"
```


##  容器部署及服务启动


```
cd /opt/app/ime-stack

WORK_PATH=/opt/app/ime-stack
```

创建 docker compose:
```
在 Portainer 中设置 work_path 变量
登录 Portainer，进入 Stacks 页面。

点击 “Add stack” 按钮。

为 stack 命名（例如 ime-stack）。

将上面修改后的 docker-compose.yml 内容粘贴到 “Web editor” 中。

在页面下方找到 “Environment variables” 部分（可能位于“Advanced options”或直接可见）。

点击 “Add environment variable”。

Name 填写： WORK_PATH

Value 填写：您希望映射的宿主机绝对路径，例如 /data/ime 或 /opt/ime。

如果需要设置多个变量，可以继续添加。

确认其他设置（如网络、Swarm 等）符合您的环境，然后点击 “Deploy the stack”。

https://via.placeholder.com/500x200?text=Portainer+Environment+Variables+Example
(示意图：在 Portainer 中添加环境变量 work_path)
```

服务报错：
```
2026-03-17 08:49:31 | INFO     | api.main:add_request_id:35 | [b5f843be] POST /api/v1/match
2026-03-17 08:49:31 | ERROR    | api.routers.match:match_resources:93 | [b5f843be] Match error: Authentication required.
INFO:     117.25.164.2:2053 - "POST /api/v1/match HTTP/1.1" 500 Internal Server Error
```
配置 one-api 中的embedding模型：

使用 大兴服务器的 ollama_cpu

配置 Redis

```
docker pull registry.cn-hangzhou.aliyuncs.com/xmxoxo_test/redis:8.0
docker tag registry.cn-hangzhou.aliyuncs.com/xmxoxo_test/redis:8.0  redis:8.0
```

启动容器服务：
```
mkdir -p /opt/data/redis/data

docker run --name redis_svr \
	-p 6379:6379 \
	-v /opt/data/redis/data:/data \
	--restart always \
	-d redis:8.0
```

报错信息：
```
2026-03-17 09:30:33 | ERROR    | api.utils.llm_client:chat_completion:56 | LLM chat completion error: Connection error.
2026-03-17 09:30:33 | ERROR    | api.core.query_parser:parse:80 | Query parsing error: Connection error.
2026-03-17 09:30:35 | ERROR    | api.core.embedding:get_embedding:63 | Embedding error: Connection error.
2026-03-17 09:30:35 | ERROR    | api.core.matcher:match_v2:217 | Match v2 error: Connection error.
2026-03-17 09:30:35 | ERROR    | api.routers.match:match_resources:93 | [56845e78] Match error: Connection error.
INFO:     117.25.164.2:2058 - "POST /api/v1/match HTTP/1.1" 500 Internal Server Error
```

测试连接参数：
```
docker exec -it ime_tool bash
docker attach ime_tool

python tools/test_llm_connection.py
```

